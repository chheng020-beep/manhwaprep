# models download on first run — never commit them
models/
*.onnx
nllb-600m-ct2/
# …except the small PP-OCR models we vendor for the Windows typeset build
!manhwaprep/ocr_models/model/*.onnx

# build + venv + caches
dist/
build/
.venv/
__pycache__/
*.pyc

# runtime output + scratch
output/
_*.png
_*.log
token.txt
bot.log

.DS_Store

# local user data
recent_projects.json
.cache/
